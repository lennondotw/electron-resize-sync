{
  "name": "@electron-resize-sync/drag-edge-heuristic",
  "version": "0.0.0",
  "private": true,
  "description": "A GUESS, NOT A MEASUREMENT. Guesses which window edges a user resize drags on macOS, from the pointer.",
  "type": "module",
  "exports": {
    "./main": "./src/main.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "electron": "^44.4.5",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "electron": ">=44.4.5"
  }
}
